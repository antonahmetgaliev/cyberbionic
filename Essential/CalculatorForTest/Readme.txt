Задание: Создать калькулятор, используя Unit Tests

+ Используя Visual Studio, создать проект по шаблону Console Application.
+ Почистить проект от лишних ссылок и "юзеров";
+ Добавить проект тестов в решение;

CalculatorTests.cs:
	- Создать класс CalculatorTest;
	- Объявляем переменную _target типа Calculator (используя решарпер создаем этот класс);
	- Создаем метод void Init(); 
		- Инициализируем _target;
	- Создаем тестовый метод void SumTest();
		- Инициализируем переменную actual, вызовом метода Sum и передаем в аргументы 2 значения;
		- Вызваем метод Assert.AreEqual и передаем в аргументы ожидаемый результат и переменную actual;
	- Повторяем последний пункт с методами Sub, Mul и Div;
	- Реализуем проверку на ответ метода Div при делении на 0;

Calculator.cs:
	- Паралельно UnitTests реализововать каждый метод.