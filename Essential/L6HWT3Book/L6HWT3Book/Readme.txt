Используя Visual Studio, создайте проект по шаблону Console Application.  
Расширьте пример урока 005_Delegation, создав в классе Book, вложенный класс Notes, 
который позволит сохранять заметки читателя. 

Решение:
	Book.cs:
		+ Создать вложенный класс Notes;
        + Объявить массив _book;
        + Созать метод Book, в котором будет реализовано заполнение книги текстом;
        + Создать метод SetNotes, в котором будет реализовано обращение к методу SetNotes во вложенном классе;
        + Создать метод DeleteNotes, в котором будет реализовано обращение к методу DeleteNotes во вложенном классе;
        + Создать метод ChangeNotes, в котором будет реализовано обращение к методу ChangeNotes во вложенном классе;
        + Создать метод Show, в котором будет реализовано вывод книги на экран;
        Notes:
            + Создать метод SetNotes, в котором будет реализовано добавление заметки читателя;
            + Создать метод DeleteNotes, в котором будет реализовано удаление заметки читателя;
            + Создать метод ChangeNotes, в котором будет реализовано изменение заметки читателя;
    BookTests.cs:
        + Объявить переменную _target;
        + Инициализировать переменную _target;
        + Создать метод SetNotesTest, который протестирует работу метода;
        + Создать метод DeleteNotesTest, который протестирует работу метода;
        + Создать метод ChangeNotesTest, который протестирует работу метода;
        - Проверить работоспособность тестов;
    Program.cs:
        - Реализовать интерфейс пользователя.

      